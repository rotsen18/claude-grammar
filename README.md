# claude-grammar

Development repo for the grammar correction hook for Claude Code.

Source lives here; the installed artifact is a subset of this tree copied to
`~/.claude/hooks/grammar/` by `install.sh` (excludes `data/`, `.env`, tests,
and other dev-only files).

Status: bootstrapped. Source migration from the installed hook is next.
